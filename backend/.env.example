# Copy this file to .env and fill in values as needed.
# The application must run without any of these being set (DEMO_MODE / local fallbacks).

# Key from https://aistudio.google.com/apikey — free, no billing required
GOOGLE_API_KEY=
# Any Gemini model ID your key has access to. See
# https://ai.google.dev/gemini-api/docs/models for the current list.
GEMINI_MODEL=gemini-3.6-flash
GITHUB_TOKEN=
YOUTUBE_API_KEY=

DATABASE_URL=sqlite:///./app.db

MAX_UPLOAD_SIZE_MB=10
MAX_PROVIDER_RESUMES=25

# Comma-separated list of allowed frontend origins for CORS
FRONTEND_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# When true (or when GOOGLE_API_KEY is missing), AI-dependent features fall
# back to deterministic, clearly-labeled behavior instead of calling a live model.
DEMO_MODE=true
