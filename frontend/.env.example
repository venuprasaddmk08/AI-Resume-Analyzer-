# Copy this file to .env and adjust if your backend runs somewhere other
# than the default. Not required for local development — the app defaults
# to http://localhost:8000 if this is unset.

VITE_API_BASE_URL=http://localhost:8000
